-- This script creates an admin user
-- You should run this manually and replace with your actual admin credentials

-- First, create the admin user through Supabase Auth UI or API
-- Then update their role to admin:

-- UPDATE public.profiles 
-- SET role = 'admin' 
-- WHERE email = 'admin@luber.com';

-- Note: Make sure to create the user first through the Supabase dashboard
-- or using the auth.signUp API, then run this update query
